<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/white_sqrt" />
    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@drawable/orange_sqrt" />
    <!-- Default state -->
    <item android:drawable="@drawable/white_sqrt" />
</selector>